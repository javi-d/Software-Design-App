<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.CST338P2Movie_Schmovies"
        tools:targetApi="31">
        <activity
            android:name=".Sign_Up_Page"
            android:exported="false" />
        <activity
            android:name=".Basic_Movie_View"
            android:exported="false"
            android:label="@string/title_activity_basic_movie_view"
            android:theme="@style/Theme.CST338P2Movie_Schmovies.NoActionBar" />
        <activity
            android:name=".log_In_Page"
            android:parentActivityName=".MainActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>